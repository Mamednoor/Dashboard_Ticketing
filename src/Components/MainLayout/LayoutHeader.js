import React from 'react'
import { Layout } from 'antd'

const { Header } = Layout

const LayoutHeader = () => {
	return (
		<Header
			style={{
				width: '100%',
			}}
		></Header>
	)
}

export default LayoutHeader
